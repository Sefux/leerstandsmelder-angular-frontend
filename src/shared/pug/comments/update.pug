include ../mixins/_form
include ../mixins/_buttons
include ../mixins/_layout
include ../mixins/_style

div(layout="row" layout-wrap layout-fill)
    div(flex layout='column')
        .md-padding.type-post(flex)
            +headline('posts.edit_post')

            textarea(markdown-editor ng-model='post.body')
            +input('checkbox','comment.hidden','comments.hidden', '_sys.hidden', null)
            hr
            div(layout="row")
                span(flex)
                +button("actions.submit",'submit()','md-raised',null,null)
