include ..//mixins/_form
include ..//mixins/_buttons
include ..//mixins/_layout
include ..//mixins/_style

div(layout='column' layout-align='start center' flex layout-padding)
  md-card()
    md-card-header
      md-card-header-text(layout-align="center center")
        +headline("users.signup")
    md-card-content()
      div(layout-padding)
        div(layout="row" layout-xs="column")
          +text('email','user.email', 'users.email')
          +text('text','user.nickname', 'users.username')
        div(layout="row" layout-xs="column")
          +text('password','user.password', 'users.password')
          +text('password','user.password_confirm', 'users.password_confirm')
        div(layout="row" layout-xs="column")
          h3
            a(href="/site/terms") {{ 'users.terms' | translate  }}
        div(layout="row" layout-xs="column")
          +input('checkbox','user.accept_terms','users.accept_terms','_sys.accept_terms',false)
    md-card-actions( layout-align="start center"  style="align-self: stretch;")
      div(layout="row" layout-align="space-around start")
        span(flex)
        +button_fab('actions.submit','submit()','md-accent','fa-sign-in')
